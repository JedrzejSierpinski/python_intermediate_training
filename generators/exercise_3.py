class Iterable():
    def __init__(self, amount):
        self.amount = amount
        self.created_value = 0

    def __iter__(self):
        if self.created_value >= self.amount > -1
            print()
